# API base URL (no trailing slash). Default: http://localhost:8080
NEXT_PUBLIC_API_URL=http://localhost:8080

# Supabase (optional). When set, API routes use Postgres instead of file-store.
# Get these from Supabase Dashboard → Project settings → API.
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Resend (optional). When set, "Email Baptism Certificate" can send PDF certificates by email.
# Get API key from https://resend.com. From address can be your verified domain or onboarding@resend.dev.
# RESEND_API_KEY=re_xxxx
# RESEND_FROM_EMAIL=Church Registry <onboarding@resend.dev>
